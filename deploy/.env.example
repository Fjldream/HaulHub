# Server public port. Change this when packaging/deploying if the port is occupied.
HTTP_PORT=8088

# Browser-facing API path for admin-web. Keep /api when using deploy/nginx.conf.
NEXT_PUBLIC_API_BASE_URL=/api
